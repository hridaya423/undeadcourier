# Undead Courier

Unity project for Undead Courier.

## Setup

- Unity: `6000.5.2f1`
- Git LFS is required for large models, textures, audio, archives, and Unity asset data under `Assets/`.
- After cloning, run `git lfs install` once, then open the project in Unity.
